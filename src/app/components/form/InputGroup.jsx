export const InputGroup = ({ children }) => {
  return (
    <div
      style={{
        marginBottom: '.75rem',
        width: '100%',
        display: 'flex',
        flexDirection: 'column',
      }}
    >
      {children}
    </div>
  )
}
